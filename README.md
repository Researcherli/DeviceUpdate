# 3-DeviceUpdate

设备固件升级工具的**发布仓库**。

本仓库只存放编译好的安装包，源码不在这里。请到 [Releases](../../releases)
页面下载最新版本。

## 关于自动更新

软件内置自动更新：打开「设置」页 → 点「检查更新」→ 有新版时点「立即更新并重启」，
程序会自己下载、替换文件并重新启动。

> 发布新版本时，请在 Releases 页面新建 Release，tag 填 `v<版本号>`（例如 `v1.0.3`），
> 并把构建好的 zip 作为附件上传。**不要勾选 Pre-release**，否则客户端取不到 `latest`。
